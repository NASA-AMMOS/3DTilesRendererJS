import { LoadingManager } from 'three';

export class SUBTREELoader {

	constructor( manager : LoadingManager, tile, rootTile );
	load( url : String ) : void ;
	parse( buffer : ArrayBuffer ) : void ;

}
